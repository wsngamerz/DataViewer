{
    "$schema": "http://json-schema.org/schema",
    "cli": "nx",
    "$id": "plugin-generator",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Plugin name",
            "$default": {
                "$source": "argv",
                "index": 0
            }
        }
    },
    "required": ["name"]
}
